/**
 * pending is use when fecting first time
 */
export declare module BookingContext {
    type TCreateBookingContext = {
        booking: Booking,
        setBooking: React.Dispatch<SetStateAction<Booking>>
    }
}